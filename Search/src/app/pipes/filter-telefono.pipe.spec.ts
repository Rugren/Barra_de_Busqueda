import { FilterTelefonoPipe } from './filter-telefono.pipe';

describe('FilterTelefonoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTelefonoPipe();
    expect(pipe).toBeTruthy();
  });
});
